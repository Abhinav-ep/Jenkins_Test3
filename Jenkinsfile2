pipeline {
    agent any

    stages {
        stage('Apply NGINX Deployment') {
            steps {
                script {
                    sh '''
                        echo "Applying nginx deployment"
                        kubectl get pods --namespace=defaul
                    '''
                }
            }
        }
    }
